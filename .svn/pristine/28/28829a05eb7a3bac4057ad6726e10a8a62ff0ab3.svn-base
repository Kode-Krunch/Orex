import { Avatar, Dialog } from 'components/ui';
import React, { useState } from 'react';
import { FaAnglesRight } from 'react-icons/fa6';
import { kpg } from 'views/Billing/BillPrinting/pdfDefinations/jso';
import VideoPlayer from 'views/Programming/ContentMaster/VideoPlayer';

function SegContentInfoDialog({ isDialogOpen, setIsDialogOpen, contentInfo }) {

  return (
    <Dialog
      isOpen={isDialogOpen}
      width={1000}
      onClose={() => setIsDialogOpen(false)}
      onRequestClose={() => setIsDialogOpen(false)}
    >
      <div className="w-full h-full  rounded-xl overflow-hidden">
        <div className="grid grid-cols-2 gap-8 p-8 h-full">
          {/* Left Side - Content Info */}
          <div className="flex flex-col space-y-6">
            {/* Header with Avatar and Content Type */}
            <div className="flex items-center">
              <Avatar size="sm" src="FAV.png" className="ring-2 ring-blue-200" />
              <span
                className="ml-3 text-sm font-semibold text-gray-500 uppercase tracking-widest"
              >
                {contentInfo.ContentType?.ContentTypeName}
              </span>
            </div>

            {/* Title */}
            <div>
              <h1 className="text-3xl font-bold text-gray-900 leading-tight font-outfit">
                {contentInfo?.ContentMaster?.ContentName}
              </h1>
            </div>


            {/* Duration */}
            <div className="flex items-center space-x-2 mt-2">
              <h3 className="text-lg font-bold text-gray-800 font-outfit flex items-center gap-2">
                Duration  <FaAnglesRight />
              </h3>
              <div className="bg-blue-100 text-blue-800 px-3 py-1 rounded-full text-sm font-semibold">
                {contentInfo?.EpisodeDurationinMin > 59 && (
                  <span>{Math.floor(contentInfo?.EpisodeDurationinMin / 60)}Hr </span>
                )}
                <span>{contentInfo?.EpisodeDurationinMin % 60}Min</span>
              </div>
            </div>

            {/* Episode Caption */}
            <div className="space-y-2 mt-5">
              <h3 className="text-xl font-bold text-gray-800 font-outfit">
                Episode Caption
              </h3>
              <p className="text-gray-500 font-semibold text-md leading-relaxed font-outfit">
                {contentInfo.EpisodeCaption}
              </p>
            </div>

            {/* Synopsis */}
            <div className="space-y-3 flex-1 mt-5">
              <h3 className="text-xl font-bold text-gray-800 font-outfit">
                Synopsis
              </h3>
              <p className="text-gray-500 font-semibold leading-relaxed font-outfit line-clamp-6">
                {contentInfo.LongSynopsis}
              </p>
            </div>

            {/* Action Buttons
            <div className="flex space-x-3 pt-4">
              <button
                onClick={() => setvideoShow(!videoShow)}
                className="bg-blue-600 hover:bg-blue-700 text-white px-6 py-2 rounded-lg font-semibold transition-colors duration-200 flex items-center space-x-2"
              >
                <svg className="w-4 h-4" fill="currentColor" viewBox="0 0 20 20">
                  <path d="M8 5v10l8-5z" />
                </svg>
                <span>Watch Trailer</span>
              </button>
              <button className="bg-gray-200 hover:bg-gray-300 text-gray-700 px-6 py-2 rounded-lg font-semibold transition-colors duration-200">
                Add to Watchlist
              </button>
            </div> */}
          </div>

          {/* Right Side - Image/Video */}
          <div className="flex items-center justify-center">
            <div className="relative w-full max-w-md">

              <div className="relative group cursor-pointer">
                <img
                  src={'./No_Image_Available.jpg'}
                  alt="Content Poster"

                />
                {/* Play Button Overlay */}

                {/* Gradient Overlay */}
              </div>

            </div>
          </div>
        </div>
      </div>
    </Dialog>
  );
}

export default SegContentInfoDialog;